# --------------------------------------------------
# Aeternum-Sentinel – Example Environment Variables
# Copy this file to `.env` (or use as env_file in Docker) and fill in your credentials.
# --------------------------------------------------

# Discord bot token
DISCORD_TOKEN=

# OAuth credentials for the web dashboard
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# Optional settings -----------------------------------------------------

# Prefix for legacy text commands (slash commands are always available)
COMMAND_PREFIX=!

# Public URL where your dashboard is reachable
# Make sure this matches your Discord application redirect URI settings.
OAUTH_REDIRECT_URI=http://localhost:8000/callback

# Bind address / port for FastAPI (inside the container)
HOST=0.0.0.0
PORT=8000 